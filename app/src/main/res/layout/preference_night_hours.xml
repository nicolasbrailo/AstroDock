<?xml version="1.0" encoding="utf-8"?>
<!-- NightHoursPreference: the default preference row, with a RangeSlider
     under the title and summary instead of a widget at the end -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:attr/listPreferredItemHeightSmall"
    android:gravity="center_vertical"
    android:paddingStart="?android:attr/listPreferredItemPaddingStart"
    android:paddingEnd="?android:attr/listPreferredItemPaddingEnd"
    android:clipToPadding="false"
    android:baselineAligned="false">

    <!-- The icon space every other row reserves, so the titles line up -->
    <FrameLayout
        android:id="@android:id/icon_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="56dp"
        android:paddingStart="0dp"
        android:paddingEnd="8dp">

        <ImageView
            android:id="@android:id/icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:importantForAccessibility="no" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="16dp"
        android:paddingBottom="16dp">

        <TextView
            android:id="@android:id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:textAppearance="?android:attr/textAppearanceListItem"
            android:ellipsize="marquee" />

        <TextView
            android:id="@android:id/summary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAlignment="viewStart"
            android:textColor="?android:attr/textColorSecondary"
            android:textAppearance="?android:attr/textAppearanceListItemSecondary"
            android:maxLines="10" />

        <!-- valueFrom and valueTo must match NightHours.AXIS; the knobs and
             the gap between them are set in code. Coloured like the
             SeekBarPreference sliders above it. -->
        <com.google.android.material.slider.RangeSlider
            android:id="@+id/night_hours_slider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stepSize="1"
            android:valueFrom="12"
            android:valueTo="35"
            app:haloColor="?attr/colorSecondary"
            app:thumbColor="?attr/colorSecondary"
            app:tickColorActive="?attr/colorOnSecondary"
            app:trackColorActive="?attr/colorSecondary" />
    </LinearLayout>
</LinearLayout>
